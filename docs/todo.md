# Worker Portal TODO

## Legacy Worker Parity

- [x] Add legacy voiceover speed-up support to the active portal worker.
  - Legacy source: `worker/processing_container/pd-050-voiceover.py` applies `ffmpeg -filter:a atempo=<speedup>` to generated TTS chunks.
  - Legacy defaults: `speedup_value` is `1.2` in `worker/processing_container/parameters.json`; project params can override with `voiceover_tempo`.
  - Current gap: `worker/local_worker.py` synthesizes and mixes raw TTS chunks without `speedup_value`, `voiceover_tempo`, or `atempo`.
  - Desired behavior: apply tempo adjustment before mixing, default to legacy `1.2`, expose an API/UI override, and keep custom recordings at `1.0`.

- [x] Add `voiceover_shift` support.
  - Legacy source: `pd-050-voiceover.py` reads `voiceover_shift` from project params before combining audio.
  - Current gap: portal mixing has no per-project global voiceover shift control.

- [x] Restore optional ElevenLabs TTS provider selection.
  - Legacy source: `pd-050-voiceover.py` supports `tts_api=elevenlabs`.
  - Current gap: the portal reports `ELEVENLABS_API_KEY` presence but always writes `tts_api: openai` and uses OpenAI TTS.

- [x] Restore custom/manual recording workflow.
  - Legacy source: `pd-005-preprocess.py` and `pd-050-voiceover.py` support `custom_recording`, download/convert WebM/M4A/OGG recordings, normalize audio, remove clicks, and remove long pauses.
  - Current gap: portal only generates TTS; no manual recording upload or chunk replacement workflow.

- [x] Restore subtitle-driven processing.
  - Legacy source: `pd-007-subtitles.py` supports SRT/VTT/SBV/TXT subtitles, `custom_subtitles`, and `use_subtitles_as_is`.
  - Current gap: portal upload/API only accepts one source media file and always transcribes from audio.

- [x] Restore URL and video input preprocessing.
  - Legacy source: `pd-005-url-processing.py` and shared `video_processing`/`mp4_processing` can download URLs, extract audio, and later merge voiceover back into video.
  - Current gap: portal MVP handles local media upload and final MP3 outputs, not URL ingestion or MP4 voiceover output.

- [x] Restore video postprocessing and preview artifacts.
  - Legacy source: `pd-055-postprocess.py` creates `.voiceover.mp4` and `.voiceover_preview.mp4` with size-limited previews.
  - Current gap: portal output is audio-only.

- [x] Restore transcript chunking with legacy-style word-level pipeline.
  - Legacy source: `pd-010-02-whisper-api-transcribe.py`, `pd-020-combine.py`, and `pd-025-timesync.py` split audio, preserve word timings, align chunks, and support long-file `hhmmss`.
  - Current behavior: portal can split transcription audio by max length or silence, request detailed word timestamps, recombine words into sentence/chunks, and keep long-file `hhmmss` output.

- [x] Restore speaker-aware proofread/timesync alignment.
  - Legacy source: `pd-025-timesync.py` detects speakers from proofread transcript text, splits by speaker, aligns proofread words back to raw word timings, and fills missing timings.
  - Current state: the portal worker now runs the legacy orchestrator path directly, so the legacy timesync stage is back in the processing flow.

- [x] Restore customization stage behavior.
  - Legacy source: `pd-035-customize.py` can generate custom improvement instructions before `pd-040-improve.py`.
  - Current gap: portal passes free-form custom instructions directly to the improve step; it does not run the autogenerated customization analysis.

- [x] Restore final audio normalization/limiting options.
  - Legacy source: `pd-050-voiceover.py` and `pd-051-ffmpeg-norm.sh` include normalization/limiting paths, although the current legacy code comments out whole-file normalization in one path.
  - Current gap: portal only lowers the background by `VOICEOVER_BACKGROUND_REDUCTION_DB` and exports MP3; it does not expose normalization/limiter settings.

- [ ] Restore full legacy parameter surface where still relevant.
  - Examples still not surfaced: `sleep_time_tts`, `openai_model`, `openai_model_tts`, `whisper_default_model_local`, `window_size_for_timesync`, `time_delta_max_for_timesync`, `deepl_delay`, `whisper_api_max_size_mp3`, `number_of_issues_to_find`, `improve_openai_model`, and `improve_openai_temperature`.
  - Current gap: the portal covers the main workflow params and some legacy defaults, but not every `parameters.json` option yet.
